# Today's update

1. Learned about Git/GitHub
2. Git command
```
    git init
    git status
    git add .
    git commit -m "msg"
    git push origin main
```
3. GitHub CLI
```
    gh auth login
    gh repo create
```